import React, { Component } from 'react';
import { Text, View, Button } from 'react-native';

export default class ConversasScreen extends Component {
    render() {
        return (
            <View>

            </View>
        );
    }
}